const Svg = require('./svg');

describe('SVG Test', function () {
    it('should fail when text exceed more than 3 characters or is blank', function () {
        const svg1 = new Svg();
        const text = 'randomtext';
        
        
        
    })
    // it('should pass when text is less than 3', function () {
    //     const text = 'test';
    //     if (text.length > 3) {
    //         return false;
    //     }
    // })
})